import { ScrapingDashboard } from "@/components/admin/ScrapingDashboard";

export const metadata = {
  title: "スクレイピング管理 | 無料駐車場マップ",
};

export default function AdminPage() {
  return <ScrapingDashboard />;
}
